Module variables

    Sub Main()
        Dim nombre As String = "pepito"
        Dim tipo As String = "dni"
        Dim dni As UInt32 = 40111111
        Dim localidad As String = "posadas"
        Dim fechanac As Date = "28/01/2000"
        Dim edad As Byte = 18
        Dim altura As Single = 1.8
        Dim estadocivil As String = "casado"
        Dim argentino As Boolean = True

        Console.WriteLine(nombre)
        Console.WriteLine(tipo)
        Console.WriteLine(dni)
        Console.WriteLine(localidad)
        Console.WriteLine(fechanac)
        Console.WriteLine(edad)
        Console.WriteLine(altura)
        Console.WriteLine(estadocivil)
        Console.WriteLine(argentino)
        Console.ReadKey()
    End Sub


End Module
